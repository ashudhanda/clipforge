# ClipForge2 one-click installers.
# PyInstaller can't cross-compile, so each OS builds on its own runner.
# Pushing a tag like v0.1.0 drafts a GitHub Release with all three installers.
name: build-installers

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            ffmpeg: win
            artifact: ClipForge2-Setup.exe
          - os: macos-latest
            ffmpeg: mac
            artifact: ClipForge2.dmg
          - os: ubuntu-latest
            ffmpeg: linux
            artifact: ClipForge2-linux64.tar.gz
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install deps
        run: pip install -r requirements.txt pyinstaller

      - name: Fetch FFmpeg (Windows)
        if: matrix.ffmpeg == 'win'
        shell: powershell
        run: ./packaging/fetch-ffmpeg.ps1

      - name: Fetch FFmpeg (macOS)
        if: matrix.ffmpeg == 'mac'
        run: |
          if [ "$(uname -m)" = "arm64" ]; then ./packaging/fetch-ffmpeg.sh macos-arm64
          else ./packaging/fetch-ffmpeg.sh macos-x86_64; fi

      - name: Fetch FFmpeg (Linux)
        if: matrix.ffmpeg == 'linux'
        run: ./packaging/fetch-ffmpeg.sh linux64

      - name: PyInstaller build
        run: pyinstaller packaging/clipforge2.spec

      - name: Inno Setup installer (Windows)
        if: matrix.ffmpeg == 'win'
        shell: powershell
        run: |
          choco install innosetup -y
          iscc packaging/installer.iss
          Move-Item dist/ClipForge2-Setup-*.exe dist/ClipForge2-Setup.exe -Force

      - name: DMG (macOS)
        if: matrix.ffmpeg == 'mac'
        run: |
          hdiutil create -volname "ClipForge2" -srcfolder dist/ClipForge2.app \
            -ov -format UDZO dist/ClipForge2.dmg

      - name: Tarball (Linux)
        if: matrix.ffmpeg == 'linux'
        run: tar -czf dist/ClipForge2-linux64.tar.gz -C dist ClipForge2

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/${{ matrix.artifact }}

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - uses: softprops/action-gh-release@v2
        with:
          draft: true
          generate_release_notes: true
          files: artifacts/**/*
          body: |
            ## ClipForge2 one-click installers (unsigned)

            - **Windows:** run `ClipForge2-Setup.exe`. SmartScreen will warn
              "Unknown publisher" — click **More info → Run anyway**.
            - **macOS:** open `ClipForge2.dmg`, drag to Applications. First launch:
              right-click → **Open** (Gatekeeper, app is unsigned).
            - **Linux:** extract `ClipForge2-linux64.tar.gz` and run `ClipForge2/ClipForge2`.

            The app opens in your browser — no terminal, no Python needed.
            FFmpeg is bundled. Your YouTube keys stay on your machine.
